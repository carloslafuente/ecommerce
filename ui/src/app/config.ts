/*====================================================
Exportamos la ruta donde se almacenaran las imagenes
====================================================*/

export let PATH = {
  url: 'http://localhost:4200/assets/',
};

/*====================================================
Exportamos el endpoint de la API REST de Firebase
====================================================*/

export let API = {
  url: 'https://ecommerce-con-angular.firebaseio.com/',
};
